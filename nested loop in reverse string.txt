# include<stdio.h>
# include<string.h>
int main(){
    char str[100];
    int i,j,len;
    scanf("%s",str);
    len=strlen(str);
    for(i=len;i>=1;i--){
        for(j=0;j<i;j++){
            printf("%c",str[j]);
        }
        printf("\n");
    }
    return 0;
}